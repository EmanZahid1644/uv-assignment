[project]
name = "packaged-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
packaged-app = "packaged_app:main"
name = "packaged_app.main:name"
Reg = "packaged_app.main:Reg"
fun = "packaged_app.main:fun"
main = "packaged_app.main:main"

[build-system]
requires = ["uv_build>=0.8.13,<0.9.0"]
build-backend = "uv_build"
