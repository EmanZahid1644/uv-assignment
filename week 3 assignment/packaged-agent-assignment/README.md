Gemini Agent Example

This project demonstrates how to set up and run a conversational agent using Google Gemini (via OpenAI-compatible API calls) with the agents framework.

🚀 Features

Loads API key securely using .env

Connects to Gemini 2.5 Flash model

Runs a simple agent that responds in one line only

Uses AsyncOpenAI with OpenAI-compatible interface